[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "java -Dserver.port=$PORT -jar app.jar"
healthcheckPath = "/api/auth/verificar-email?token=test"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
